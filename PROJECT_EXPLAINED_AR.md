# دليل وبنية مشروع السراج للعطور 📚
(Project Understanding - فهم المشروع)

هذا الملف يحتوي على شرح مبسط وهيكلي للمشروع باللغة العربية، ليكون مرجعاً شاملاً لكيفية عمل الموقع، وكيفية إدارته، وأين تجد الملفات المهمة.

---

## 1. كيفية إدارة المنتجات (لوحة التحكم) 🛠️

الموقع يحتوي على لوحة تحكم متكاملة للأدمن، تمكنك من إدارة المتجر بالكامل دون الحاجة للمس الأكواد.

### الدخول للوحة التحكم 🔐
- **الرابط:** `/admin`
- عند الدخول، يمكنك مشاهدة إحصائيات سريعة أو إدارة المنتجات.

### أ. إضافة منتج جديد ➕
1.  من لوحة التحكم، اختر تبويب **"إضافة منتج"**.
2.  أدخل اسم المنتج، الوصف، والسعر.
3.  **نظام الصور الذكي 🖼️**:
    *   عند رفع صورة جديدة، يقوم النظام تلقائياً بحفظ الصورة القديمة (في حال التعديل) في ألبوم الصور، ويضع الصورة الجديدة كصورة رئيسية.
    *   هذا يضمن عدم ضياع صورك القديمة، حيث يتم دمجها في معرض الصور.
4.  **نظام الأحجام والأسعار (ديناميكي) 💰**:
    *   يمكنك إضافة أحجام متعددة (مثلاً: 50ml, 100ml) لكل منتج.
    *   لكل حجم سعر خاص وسعر قبل الخصم (اختياري).
    *   **ملاحظة هامة:** الأسعار التي تدخلها هنا هي التي ستظهر **فوراً** وبشكل ديناميكي على الموقع. أي تعديل هنا ينعكس لحظياً عند الزبائن.

### ب. تعديل وتحديث المنتجات ✏️
1.  انتقل لتبويب **"إدارة المنتجات"**.
2.  ستظهر لك قائمة بكل المنتجات الحالية.
3.  اضغط على زر **"تعديل"** بجانب أي منتج.
4.  سيتم نقل بيانات المنتج لنموذج الإضافة لتعديلها.
5.  عند الضغط على "تحديث"، يتم حفظ التغييرات (الاسم، السعر، الأحجام، الصور) واستبدال البيانات القديمة بالجديدة في قاعدة البيانات فوراً.

### ج. حذف المنتجات 🗑️
- من قائمة "إدارة المنتجات"، يمكنك حذف أي منتج نهائياً بالضغط على زر الحذف.

---

## 2. هيكلية المجلدات (أين أجد ملفاتي؟) 📂

المشروع مبني باستخدام **Next.js** مع قاعدة بيانات **MongoDB**، وهذه هي المجلدات الرئيسية:

### `app/` 🏠
هذا هو المجلد الأساسي للصفحات والمسارات (Routes).
- **`page.tsx`**: الصفحة الرئيسية للموقع.
- **`layout.tsx`**: التصميم العام (القائمة العلوية `Navbar` وتذييل الصفحة `Footer`).
- **`products/`**: صفحة عرض كل المنتجات.
- **`admin/`**: مجلد لوحة التحكم.
    - **`dashboard/`**: الصفحة الرئيسية للوحة التحكم (إضافة/تعديل/حذف).
- **`api/`**: مجلد الواجهات البرمجية (Backend) الذي يربط الموقع بقاعدة البيانات.
    - **`admin/add-product`**: كود إضافة المنتجات.
    - **`admin/edit-product`**: كود تعديل المنتجات.

### `components/` 🧩
يحتوي على العناصر القابلة لإعادة الاستخدام.
- **`Navbar.tsx`**: الشريط العلوي.
- **`Footer.tsx`**: ذيل الصفحة.
- **`ProductDetails.tsx`**: (هام) الكود المسؤول عن عرض صفحة المنتج، واختيار الأحجام، وتغيير السعر لحظياً بناءً على اختيار الزبون.

### `models/` 🗄️ (جديد)
- **`Product.ts`**: ملف تعريف شكل بيانات المنتج في قاعدة البيانات (الاسم، السعر، الصور...).

### `public/` 🖼️
- **`images/`**: يحتوي على الصور الثابتة للموقع.
- **`uploads/`**: المجلد الذي يتم حفظ صور المنتجات الجديدة فيه عند رفعها من لوحة التحكم.

---

## 3. أسئلة شائعة ❓

### س: هل تعديل السعر من الأدمن يغيره فوراً في الموقع؟
**ج:** نعم، وبشكل لحظي. بمجرد تعديل سعر حجم معين (مثلاً الـ 100 مل) وحفظ المنتج، سيظهر السعر الجديد لأي زبون يفتح الصفحة فوراً. النظام ديناميكي بالكامل.

### س: رفعت صورة جديدة، لماذا تظهر القديمة والجديدة معاً؟
**ج:** نظام الصور في الموقع مصمم حالياً ليكون "تراكمياً". عند رفع صورة جديدة أثناء التعديل، يقوم النظام بحفظ الصورة القديمة ضمن ألبوم الصور لضمان عدم فقدانها، ويضيف الصورة الجديدة كصورة رئيسية. لهذا السبب ترى الصورتين معاً.

### س: حذفت الصورة من المجلد، لماذا لا تزال تظهر؟
**ج:** قد يكون السبب أن المتصفح (Browser) يحتفظ بنسخة قديمة (Cache). جرب تحديث الصفحة بقوة (Ctrl + F5). أو قد تكون الصورة لا تزال موجودة في قاعدة البيانات (الرابط موجود) حتى لو حذف الملف، مما يؤدي لظهور "صورة مكسورة" أو النسخة المخبأة. الحل الأفضل هو حذف الصورة من قاعدة البيانات عبر الكود.

---

هذا الملف مرجع لك، ومحدث ليشمل آخر التطورات في مشروعك. بالتوفيق! 🚀
