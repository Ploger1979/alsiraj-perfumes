# دليل وبنية مشروع السراج للعطور 📚

هذا الملف يحتوي على شرح مبسط وهيكلي للمشروع باللغة العربية، لمساعدتك على فهم الخطوات والأكواد وكيفية عمل الموقع ككل.

---

## 1. هيكلية المجلدات (أين أجد ملفاتي؟) 📂

المشروع مبني باستخدام **Next.js** (أحدث نسخة)، وهذه هي المجلدات الرئيسية:

### `app/` 🏠
هذا هو المجلد الأساسي للصفحات.
- **`page.tsx`**: الصفحة الرئيسية للموقع.
- **`layout.tsx`**: التصميم العام الذي يظهر في كل الصفحات (يحتوي على القائمة العلوية `Navbar` وتذييل الصفحة `Footer`).
- **`products/`**: صفحة عرض كل المنتجات.
- **`products/[id]/`**: صفحة تفاصيل منتج معين (الرقم `[id]` يتغير حسب المنتج).
- **`cart/`**: صفحة سلة التسوق.

### `components/` 🧩
يحتوي على "قطع الليغو" أو المكونات التي نستخدمها لبناء الصفحات.
- **`Navbar.tsx`**: الشريط العلوي (القائمة).
- **`Footer.tsx`**: ذيل الصفحة (يحتوي على الحقوق والروابط).
- **`ProductDetails.tsx`**: (مهم جداً) هذا المكون هو المسؤول عن عرض تفاصيل المنتج، اختيار الحجم (50 مل / 100 مل)، وعملية الشراء.
- **`CartDrawer.tsx`**: القائمة الجانبية للسلة التي تظهر عند الضغط على أيقونة العربة.

### `data/` 💾
- **`products.ts`**: هذا هو "قاعدة البيانات" الخاصة بنا حالياً. ملف يحتوي على قائمة بكل المنتجات، أسعارها، صورها، والأحجام المتاحة.
    - **ملاحظة:** أي تعديل في الأسعار أو إضافة منتج جديد يتم هنا.

### `context/` 🧠
- **`CartContext.tsx`**: هذا الملف هو "عقل" سلة التسوق. هو المسؤول عن تذكر المنتجات التي أضفتها للسلة حتى عند انتقالك بين الصفحات.

### `utils/` 🛠️
- **`format.ts`**: يحتوي على دوال مساعدة صغيرة، مثل دالة تنسيق العملة (لإظهار الفواصل في السعر 150,000 د.ع).

---

## 2. شرح السيناريوهات الهامة (كيف يعمل الكود؟) ⚙️

### أ. كيف تعمل صفحة تفاصيل المنتج؟ (`ProductDetails.tsx`)
عندما يدخل المستخدم لصفحة منتج، تحدث الخطوات التالية:
1.  **التحميل**: يقرأ الكود بيانات المنتج من `data/products.ts`.
2.  **الحالة الافتراضية (`State`)**:
    *   نحدد **الحجم المختار** تلقائياً ليكون أول حجم (مثلاً 50 مل).
    *   نحدد **السعر** بناءً على هذا الحجم.
3.  **التفاعل**:
    *   عندما يضغط المستخدم على "100 مل"، يقوم الكود بتحديث "الحجم المختار" ويغير السعر المعروض فوراً.
4.  **الإضافة للسلة**:
    *   عند الضغط على "أضف للسلة"، نأخذ المنتج بالمعلومات الحالية (الحجم المختار وسعره) ونرسله لـ `CartContext`.

### ب. كيف تعمل سلة التسوق؟ (`CartContext.tsx`)
1.  هذا الملف يغلف التطبيق كله (`Provider`).
2.  عند إضافة منتج، يتأكد: "هل هذا المنتج موجود مسبقاً بنفس الحجم؟"
    *   **نعم**: يزيد العدد (+1).
    *   **لا**: يضيفه كمنتج جديد.
3.  يحفظ البيانات في `localStorage` (ذاكرة المتصفح) لكي لا تضيع عند تحديث الصفحة.

---

## 3. خطوات التعديل الشائعة ✏️

### أريد إضافة منتج جديد
1.  افتح ملف `data/products.ts`.
2.  انسخ كائن منتج موجود (ما بين الأقواس `{ ... }`).
3.  ألصقه وغير الـ `id` (يجب أن يكون رقم مميز) والاسم والصورة.

### أريد تعديل الأسعار
1.  افتح `data/products.ts`.
2.  ابحث عن المنتج.
3.  عدل `price` (السعر الأساسي).
4.  إذا كان له أحجام (`sizes`)، تأكد من تعديل السعر داخل مصفوفة `sizes` أيضاً.

### أريد تغيير ألوان الموقع
1.  افتح `app/globals.css`.
2.  هنا توجد الألوان الأساسية للموقع والخطوط.

---

هذا الملف مرجع لك في أي وقت، ومكتوب ليكون سهلاً وواضحاً. بالتوفيق في مشروعك! 🚀
